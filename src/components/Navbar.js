import React from 'react'

export const Navbar = () => {
  return (
    <div>
        <nav className="navbar">
            <a href="#">Inicio</a>
            <a href="#">Servicio</a>
            <a href="#">Precios</a>
            <a href="#">Contacto</a>
        </nav>
    </div>

  )
}

export default Navbar